/*
 * Created on 2026-04-24 ( 22:23:49 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.3.0
 */
package ufps.edu.co.services;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import ufps.edu.co.persistence.entities.TituloEntity;
import ufps.edu.co.persistence.repositories.TituloRepository;
import ufps.edu.co.rest.dto.TituloDTO;
import ufps.edu.co.services.commons.GenericService;

/**
 * REST service for entity "Titulo" <br>
 *
 * @author Telosys
 */
@Service
@Transactional
public class TituloService extends GenericService<TituloEntity, TituloDTO> {

    @Autowired
    private TituloRepository repository;

    public TituloService() {
        super(TituloEntity.class, TituloDTO.class);
    }

    @Transactional(readOnly = true)
    public List<TituloDTO> findAll() {
        return entityListToDtoList(repository.findAll());
    }

    @Transactional(readOnly = true)
    public TituloDTO findById(int id) {
        return entityToDto(repository.findById(id));
    }

    public TituloDTO create(TituloDTO dto) {
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public TituloDTO update(int id, TituloDTO dto) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Titulo no encontrado con id: " + id));
        dto.setId(id);
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public void deleteById(int id) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Titulo no encontrado con id: " + id));
        repository.deleteById(id);
    }
}