/*
 * Created on 2026-04-24 ( 22:23:47 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.3.0
 */
package ufps.edu.co.services;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import ufps.edu.co.persistence.entities.EstadoAspiranteEntity;
import ufps.edu.co.persistence.repositories.EstadoAspiranteRepository;
import ufps.edu.co.rest.dto.EstadoAspiranteDTO;
import ufps.edu.co.services.commons.GenericService;

/**
 * REST service for entity "EstadoAspirante" <br>
 *
 * @author Telosys
 */
@Service
@Transactional
public class EstadoAspiranteService extends GenericService<EstadoAspiranteEntity, EstadoAspiranteDTO> {

    @Autowired
    private EstadoAspiranteRepository repository;

    public EstadoAspiranteService() {
        super(EstadoAspiranteEntity.class, EstadoAspiranteDTO.class);
    }

    @Transactional(readOnly = true)
    public List<EstadoAspiranteDTO> findAll() {
        return entityListToDtoList(repository.findAll());
    }

    @Transactional(readOnly = true)
    public EstadoAspiranteDTO findById(int id) {
        return entityToDto(repository.findById(id));
    }

    public EstadoAspiranteDTO create(EstadoAspiranteDTO dto) {
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public EstadoAspiranteDTO update(int id, EstadoAspiranteDTO dto) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("EstadoAspirante no encontrado con id: " + id));
        dto.setId(id);
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public void deleteById(int id) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("EstadoAspirante no encontrado con id: " + id));
        repository.deleteById(id);
    }
}