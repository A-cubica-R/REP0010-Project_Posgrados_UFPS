/*
 * Created on 2026-04-24 ( 22:23:49 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.3.0
 */
package ufps.edu.co.services;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import ufps.edu.co.persistence.entities.TipoTituloEntity;
import ufps.edu.co.persistence.repositories.TipoTituloRepository;
import ufps.edu.co.rest.dto.TipoTituloDTO;
import ufps.edu.co.services.commons.GenericService;

/**
 * REST service for entity "TipoTitulo" <br>
 *
 * @author Telosys
 */
@Service
@Transactional
public class TipoTituloService extends GenericService<TipoTituloEntity, TipoTituloDTO> {

    @Autowired
    private TipoTituloRepository repository;

    public TipoTituloService() {
        super(TipoTituloEntity.class, TipoTituloDTO.class);
    }

    @Transactional(readOnly = true)
    public List<TipoTituloDTO> findAll() {
        return entityListToDtoList(repository.findAll());
    }

    @Transactional(readOnly = true)
    public TipoTituloDTO findById(int id) {
        return entityToDto(repository.findById(id));
    }

    public TipoTituloDTO create(TipoTituloDTO dto) {
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public TipoTituloDTO update(int id, TipoTituloDTO dto) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("TipoTitulo no encontrado con id: " + id));
        dto.setId(id);
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public void deleteById(int id) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("TipoTitulo no encontrado con id: " + id));
        repository.deleteById(id);
    }
}