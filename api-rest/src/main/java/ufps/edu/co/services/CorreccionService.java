/*
 * Created on 2026-04-24 ( 22:23:47 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.3.0
 */
package ufps.edu.co.services;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import ufps.edu.co.persistence.entities.CorreccionEntity;
import ufps.edu.co.persistence.repositories.CorreccionRepository;
import ufps.edu.co.rest.dto.CorreccionDTO;
import ufps.edu.co.services.commons.GenericService;

/**
 * REST service for entity "Correccion" <br>
 *
 * @author Telosys
 */
@Service
@Transactional
public class CorreccionService extends GenericService<CorreccionEntity, CorreccionDTO> {

    @Autowired
    private CorreccionRepository repository;

    public CorreccionService() {
        super(CorreccionEntity.class, CorreccionDTO.class);
    }

    @Transactional(readOnly = true)
    public List<CorreccionDTO> findAll() {
        return entityListToDtoList(repository.findAll());
    }

    @Transactional(readOnly = true)
    public CorreccionDTO findById(int id) {
        return entityToDto(repository.findById(id));
    }

    public CorreccionDTO create(CorreccionDTO dto) {
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public CorreccionDTO update(int id, CorreccionDTO dto) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Correccion no encontrado con id: " + id));
        dto.setId(id);
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public void deleteById(int id) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Correccion no encontrado con id: " + id));
        repository.deleteById(id);
    }
}