/*
 * Created on 2026-04-24 ( 22:23:47 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.3.0
 */
package ufps.edu.co.services;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import ufps.edu.co.persistence.entities.DocumentoIdentidadEntity;
import ufps.edu.co.persistence.repositories.DocumentoIdentidadRepository;
import ufps.edu.co.rest.dto.DocumentoIdentidadDTO;
import ufps.edu.co.services.commons.GenericService;

/**
 * REST service for entity "DocumentoIdentidad" <br>
 *
 * @author Telosys
 */
@Service
@Transactional
public class DocumentoIdentidadService extends GenericService<DocumentoIdentidadEntity, DocumentoIdentidadDTO> {

    @Autowired
    private DocumentoIdentidadRepository repository;

    public DocumentoIdentidadService() {
        super(DocumentoIdentidadEntity.class, DocumentoIdentidadDTO.class);
    }

    @Transactional(readOnly = true)
    public List<DocumentoIdentidadDTO> findAll() {
        return entityListToDtoList(repository.findAll());
    }

    @Transactional(readOnly = true)
    public DocumentoIdentidadDTO findById(int id) {
        return entityToDto(repository.findById(id));
    }

    public DocumentoIdentidadDTO create(DocumentoIdentidadDTO dto) {
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public DocumentoIdentidadDTO update(int id, DocumentoIdentidadDTO dto) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("DocumentoIdentidad no encontrado con id: " + id));
        dto.setId(id);
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public void deleteById(int id) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("DocumentoIdentidad no encontrado con id: " + id));
        repository.deleteById(id);
    }
}