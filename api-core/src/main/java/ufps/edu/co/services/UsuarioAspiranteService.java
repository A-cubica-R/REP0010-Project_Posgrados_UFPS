/*
 * Created on 2026-04-24 ( 22:23:49 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.3.0
 */
package ufps.edu.co.services;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import ufps.edu.co.persistence.entities.UsuarioAspiranteEntity;
import ufps.edu.co.persistence.repositories.UsuarioAspiranteRepository;
import ufps.edu.co.rest.dto.UsuarioAspiranteDTO;
import ufps.edu.co.services.commons.GenericService;

/**
 * REST service for entity "UsuarioAspirante" <br>
 *
 * @author Telosys
 */
@Service
@Transactional
public class UsuarioAspiranteService extends GenericService<UsuarioAspiranteEntity, UsuarioAspiranteDTO> {

    @Autowired
    private UsuarioAspiranteRepository repository;

    public UsuarioAspiranteService() {
        super(UsuarioAspiranteEntity.class, UsuarioAspiranteDTO.class);
    }

    @Transactional(readOnly = true)
    public List<UsuarioAspiranteDTO> findAll() {
        return entityListToDtoList(repository.findAll());
    }

    @Transactional(readOnly = true)
    public UsuarioAspiranteDTO findById(int idUsuario) {
        return entityToDto(repository.findById(idUsuario));
    }

    public UsuarioAspiranteDTO create(UsuarioAspiranteDTO dto) {
        if (repository.existsById(dto.getIdUsuario())) {
            throw new RuntimeException("UsuarioAspirante ya existe con id: " + dto.getIdUsuario());
        }
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public UsuarioAspiranteDTO update(int idUsuario, UsuarioAspiranteDTO dto) {
        repository.findById(idUsuario)
                .orElseThrow(() -> new RuntimeException("UsuarioAspirante no encontrado con id: " + idUsuario));
        dto.setIdUsuario(idUsuario);
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public void deleteById(int idUsuario) {
        repository.findById(idUsuario)
                .orElseThrow(() -> new RuntimeException("UsuarioAspirante no encontrado con id: " + idUsuario));
        repository.deleteById(idUsuario);
    }
}