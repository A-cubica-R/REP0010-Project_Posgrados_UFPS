/*
 * Created on 2026-05-14 ( 22:38:00 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.3.0
 */
package ufps.edu.co.rest.services;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import ufps.edu.co.persistence.entities.EstadocivilEntity;
import ufps.edu.co.persistence.repositories.EstadocivilRepository;
import ufps.edu.co.rest.dto.EstadocivilDTO;
import ufps.edu.co.rest.services.commons.GenericService;

/**
 * REST service for entity "Estadocivil" <br>
 *
 * @author Telosys
 */
@Service
@Transactional
public class EstadocivilService extends GenericService<EstadocivilEntity, EstadocivilDTO> {

    @Autowired
    private EstadocivilRepository repository;

    public EstadocivilService() {
        super(EstadocivilEntity.class, EstadocivilDTO.class);
    }

    @Transactional(readOnly = true)
    public List<EstadocivilDTO> findAll() {
        return entityListToDtoList(repository.findAll());
    }

    @Transactional(readOnly = true)
    public EstadocivilDTO findById(Integer id) {
        return entityToDto(repository.findById(id));
    }

    public EstadocivilDTO create(EstadocivilDTO dto) {
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public EstadocivilDTO update(Integer id, EstadocivilDTO dto) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Estadocivil no encontrado con id: " + id));
        dto.setId(id);
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public void deleteById(Integer id) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Estadocivil no encontrado con id: " + id));
        repository.deleteById(id);
    }
}