/*
 * Created on 2026-05-18 ( 01:57:52 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.3.0
 */
package ufps.edu.co.rest.services;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import ufps.edu.co.persistence.entities.GrupoetnicoEntity;
import ufps.edu.co.persistence.repositories.GrupoetnicoRepository;
import ufps.edu.co.rest.dto.GrupoetnicoDTO;
import ufps.edu.co.rest.services.commons.GenericService;

/**
 * REST service for entity "Grupoetnico" <br>
 *
 * @author Telosys
 */
@Service
@Transactional
public class GrupoetnicoService extends GenericService<GrupoetnicoEntity, GrupoetnicoDTO> {

    @Autowired
    private GrupoetnicoRepository repository;

    public GrupoetnicoService() {
        super(GrupoetnicoEntity.class, GrupoetnicoDTO.class);
    }

    @Transactional(readOnly = true)
    public List<GrupoetnicoDTO> findAll() {
        return entityListToDtoList(repository.findAll());
    }

    @Transactional(readOnly = true)
    public GrupoetnicoDTO findById(Integer id) {
        return entityToDto(repository.findById(id));
    }

    public GrupoetnicoDTO create(GrupoetnicoDTO dto) {
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public GrupoetnicoDTO update(Integer id, GrupoetnicoDTO dto) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Grupoetnico no encontrado con id: " + id));
        dto.setId(id);
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public void deleteById(Integer id) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Grupoetnico no encontrado con id: " + id));
        repository.deleteById(id);
    }
}