/*
 * Created on 2026-05-24 ( 16:57:22 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.3.0
 */
package ufps.edu.co.rest.services;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import ufps.edu.co.persistence.entities.TipodocumentopersonaEntity;
import ufps.edu.co.persistence.repositories.TipodocumentopersonaRepository;
import ufps.edu.co.rest.dto.TipodocumentopersonaDTO;
import ufps.edu.co.rest.services.commons.GenericService;

/**
 * REST service for entity "Tipodocumentopersona" <br>
 *
 * @author Telosys
 */
@Service
@Transactional
public class TipodocumentopersonaService extends GenericService<TipodocumentopersonaEntity, TipodocumentopersonaDTO> {

    @Autowired
    private TipodocumentopersonaRepository repository;

    public TipodocumentopersonaService() {
        super(TipodocumentopersonaEntity.class, TipodocumentopersonaDTO.class);
    }

    @Transactional(readOnly = true)
    public List<TipodocumentopersonaDTO> findAll() {
        return entityListToDtoList(repository.findAll());
    }

    @Transactional(readOnly = true)
    public TipodocumentopersonaDTO findById(Integer id) {
        return entityToDto(repository.findById(id));
    }

    public TipodocumentopersonaDTO create(TipodocumentopersonaDTO dto) {
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public TipodocumentopersonaDTO update(Integer id, TipodocumentopersonaDTO dto) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Tipodocumentopersona no encontrado con id: " + id));
        dto.setId(id);
        return entityToDto(repository.save(dtoToEntity(dto)));
    }

    public void deleteById(Integer id) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Tipodocumentopersona no encontrado con id: " + id));
        repository.deleteById(id);
    }
}