/*
 * Created on 2026-05-18 ( 01:57:55 )
 * Generated by Telosys ( https://www.telosys.org/ ) version 4.3.0
 */
package ufps.edu.co.rest.services;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import ufps.edu.co.persistence.entities.TipoentrevistaEntity;
import ufps.edu.co.persistence.repositories.TipoentrevistaRepository;
import ufps.edu.co.rest.dto.TipoentrevistaDTO;
import ufps.edu.co.rest.services.commons.GenericService;

/**
 * REST service for entity "Tipoentrevista" <br>
 *
 * @author Telosys
 */
@Service
@Transactional
public class TipoentrevistaService extends GenericService<TipoentrevistaEntity, TipoentrevistaDTO> {

    @Autowired
    private TipoentrevistaRepository repository;

    public TipoentrevistaService() {
        super(TipoentrevistaEntity.class, TipoentrevistaDTO.class);
    }

    private TipoentrevistaDTO toDto(TipoentrevistaEntity entity) {
        if (entity == null) {
            return null;
        }

        return TipoentrevistaDTO.builder()
                .id(entity.getId())
                .descripcion(entity.getDescripcion())
                .tipo(entity.getTipo())
                .build();
    }

    @Transactional(readOnly = true)
    public List<TipoentrevistaDTO> findAll() {
        return repository.findAll().stream().map(this::toDto).toList();
    }

    @Transactional(readOnly = true)
    public TipoentrevistaDTO findById(Integer id) {
        return repository.findById(id).map(this::toDto).orElse(null);
    }

    public TipoentrevistaDTO create(TipoentrevistaDTO dto) {
        return toDto(repository.save(dtoToEntity(dto)));
    }

    public TipoentrevistaDTO update(Integer id, TipoentrevistaDTO dto) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Tipoentrevista no encontrado con id: " + id));
        dto.setId(id);
        return toDto(repository.save(dtoToEntity(dto)));
    }

    public void deleteById(Integer id) {
        repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Tipoentrevista no encontrado con id: " + id));
        repository.deleteById(id);
    }

    @Transactional(readOnly = true)
    public TipoentrevistaDTO findByTipo(String tipo) {
        return repository.findByTipoIgnoreCase(tipo).map(this::toDto).orElse(null);
    }
}